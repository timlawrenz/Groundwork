// Sentry auto-initializes via Tools → Sentry window (SentryOptions.asset).
// Manual init code removed — SentrySdk is internal in the Unity Sentry SDK.
// Re-enable if needed: use SentryWindow in the Unity Editor to configure.